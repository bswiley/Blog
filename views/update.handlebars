<h2>Post Update</h2>
<p>
  <span>Make the corrections to your post in the blanks below and hit the "Update" button</span>
  <span class="hide">{{post.id}}</span>
</p>

<form class="form create-form">
  <div class="form-group">
    <label for="post-title">Title</label>
    <input class="form-input" type="text" id="post-title" value="{{post.title}}">
  </div>
  <div class="form-group">
    <label for="post-text">Post Text</label>
    <textarea class="form-input" id="post-text" rows="20">{{post.text}}</textarea>
  </div>

  <div class="form-group">
    <button class="btn create" id="update" type="button">Update</button>
  </div>
</form>

<script src="/js/update.js"></script>

